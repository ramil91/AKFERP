/** Single JSON blob: AuthSession + metadata; see authStorage helper. */
export const AUTH_STORAGE_KEY = 'akferp.auth.session';

/** Theme preference: 'light' | 'dark' | 'system' — see ThemeContext. */
export const THEME_STORAGE_KEY = 'akferp.theme.preference';
