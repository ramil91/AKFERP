# Base URL for API calls. In dev, leave empty to use Vite proxy (/api -> AKFERP.API).
VITE_API_URL=

# Set to "true" to use in-browser mock auth (no backend). Set to "false" to call real /api/auth/*.
VITE_USE_MOCK_AUTH=true
